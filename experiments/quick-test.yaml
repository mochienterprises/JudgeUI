# Quick Test Experiment
#
# A minimal experiment for testing the framework.
# Uses a single model with default settings.

name: "quick-test"
description: "Quick validation test"

matrix:
  models:
    - claude-sonnet-4-5

  temperatures:
    - 1.0

  evaluator_prompts:
    - default

argument_ids: []

runs_per_combination: 1
